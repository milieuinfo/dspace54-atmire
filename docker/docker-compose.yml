version: "3"
services:
  postgres:
    image: postgres:11
    ports:
    - "5432:5432"
    environment:
    - POSTGRES_USER=dspace
    - POSTGRES_DB=dspace
    - POSTGRES_PASSWORD=dspace
    volumes:
    - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
